import React from 'react'
import { Text, View } from 'react-native';

export default function Achievement({ name }) {
    return (
        <View>
            <Text> {name}</Text>
        </View>
    )
}
